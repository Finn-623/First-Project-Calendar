# v0.1.3

## 版本名称

v0.1.3 — 基础记录闭环与稳定性收尾

## 版本状态

development（上线前检查完成，待发布）

## 上线时间

待正式上线确认（status=development，releasedAt=null）

## 版本目标

- 完成 v0.1 阶段核心记录链路的最终收尾。
- 以修复缺口和补齐验证为主，不扩展新模块。

## 纳入范围

- 登录与退出账号。
- 首页饮食记录新增、编辑、删除。
- 删除全部食物后自动删除空餐。
- 结束本日和自动进入下一记录日。
- 首页真实日期、当前记录日和已结束状态规则。
- 删除本日历史记录后首页返回真实本日。
- 历史记录查看、编辑、单条删除和批量删除。
- 设置入口进入历史记录。
- 底部导航只保留首页、食物库、设置。
- 食物库基础功能和用户数据权限。
- 基础计划功能。
- 刷新、重新登录后的数据与页面状态。
- 手机端主要页面。
- Production 是否为最新部署版本。

## 明确不纳入

- 统计功能扩展。
- 模板。
- AI。
- 离线数据库。
- 图片体系扩展。
- GI/GL 扩展。
- 健身模块扩展。
- 记账模块。

## 当前完成情况（代码与测试证据核对）

### 已完成，可直接验收

- 登录、退出、会话恢复主链路已实现。
- 设置页账号区域的退出账号闭环已完成；退出成功后清理私有运行时状态并返回普通登录页。
- “结束本日／删除本日历史／返回首页”关键路径回归已完成，覆盖日期推进、完成状态、删除隔离、取消/失败和重新初始化。
- 刷新与重新登录状态一致性回归已完成，覆盖 session 恢复等待、无 session 降级、日期恢复、同账号数据重载、跨账号隔离、失败状态与重复认证回调。
- 已修复切换账号时旧 profile 短暂保留，以及旧账号 history/plan 异步响应迟到覆盖新账号数据的竞态。
- 手机端主要页面验收已完成，覆盖 320×568、375×667、390×844、430×932 四档 viewport。
- 已修复添加食物 Sheet 在矮屏的滚动与动态高度、通用弹窗超出动态视口、长食物名称挤压操作区和首页删除入口点击面积偏小的问题。
- 首页当日食物删除与空餐自动清理已实现；删除成功后会立即更新餐次及当日热量、蛋白质、脂肪和碳水汇总。
- 结束本日推进与首页日期动态规则已实现。
- 删除本日历史后首页回到真实本日已实现。
- 历史记录查看、编辑、单条删除、批量删除已实现。
- 设置入口进入历史记录已实现。
- 底部导航三入口已实现。
- 食物库基础能力与权限主链路已实现。
- 基础计划功能已实现。

### 已实现但需要修复或补充测试

- 无仓库内已知上线阻塞项；Production 当前仍是 v0.1.2，正式部署与线上验收尚未执行。

### 尚未实现，需要纳入 v0.1.3

- 无；本地发布前检查已完成。

### 不属于 v0.1.3，移入后续版本

- 统计增强。
- 模板。
- AI。
- 离线数据库与同步。
- 图片体系扩展。
- GI/GL 扩展。
- 健身模块扩展。
- 记账模块。

## 待处理任务清单（按优先级）

### P0

- 已完成：退出账号、首页食物删除、结束本日／删除本日历史／返回首页关键路径均有自动化回归。

### P1

- 已完成：刷新、重新登录后的状态一致性回归。
- 已完成：手机端主要页面验收记录。

### P2

- 已完成：Production 版本一致性核对流程与发布前检查清单。

## 上线验收标准

- P0 与 P1 全部完成并有测试或人工验收记录。
- 版本文档、版本索引、项目状态文档一致。
- 完成生产环境版本一致性核对。
- 仅在正式上线后回填上线时间与 releasedAt。

## Commit ID 回填位置

- 发布范围确认主提交：6dd2e5d。
- 文档回填提交：a63af746a2eca5c3ccaca0bd5109778b09027a15。

## 正式上线时间回填位置

- 本文件“上线时间”章节。
- frontend/src/config/version.config.json 的 releasedAt 字段（仅正式上线后填写）。

## 相关提交

- 范围确认提交：6dd2e5d
- 首页当日食物删除与空餐清理：deb10ece832d4f4dcee6bf34a78c486f03d96546
- 退出账号功能：746daeb04c7e390588f13d6984a9700f823eea41
- 结束本日与历史删除回归：f132b6c3c9aa3f54454c8a54b464587c01e291f9
- 刷新与重新登录状态一致性：81860a472efdb3b048ef1eba2c8d6c03a93b59c0
- 手机端主要页面验收：a0eccfbc602ff09c5b2e7f034dead1026b278749
- 发布前版本一致性与修改建议 Review：fcf5f6dcc5199992ed38d16bf20962b050cc20ef
- 回填提交：6890ba05d063d540c6e5a39043074ba3a8365756

## 手机端主要页面验收记录

- 验收 viewport：320×568、375×667、390×844、430×932。
- 验收方式：固定 viewport 的组件渲染与交互回归、现有页面测试、响应式结构与公共样式审查、生产构建；未连接生产数据库，未冒充真实设备人工测试。
- 登录页：通过。输入框、登录按钮和错误区域结构完整，不存在“切换账号”或专用切换提示。
- 首页：已修复后通过。日期、营养卡片和时间轴保留底部空间；长食物名称可换行，删除按钮扩大为 40×40px 并保持可点击；删除确认操作完整。
- 添加食物 Sheet：已修复后通过。使用动态视口高度与最大高度，列表及选中食物后的克重/确认区域可滚动，超长名称不挤压热量与操作区。
- 历史记录列表：通过。日期、批量删除入口和空状态结构正常，页面使用 `pb-32` 避免底部导航覆盖。
- 历史详情：已修复后通过。查看、编辑、删除入口可操作；长内容沿用时间轴换行规则；公共确认弹窗具备动态最大高度与内部滚动。
- 食物库：已修复后通过。分类条使用自身横向滚动，不造成页面横向溢出；超长食物名称与营养文字可换行；添加/编辑表单通过通用 Dialog 的动态高度和滚动保护保持可提交。
- 计划页面：通过。热量单行与三列宏量输入均使用可收缩宽度，结果和保存区域保留底部空间。
- 设置页面：通过。设置项采用可收缩文本区与固定图标；历史记录和退出账号入口可点击，不显示切换账号。
- 退出账号确认：已修复后通过。取消和退出操作完整，弹窗不会超过动态视口且内容可滚动。
- 底部导航：通过。仅首页、食物库、设置三个等宽入口，具有安全区处理；主要页面均预留底部空间，不存在历史记录入口。
- 横向溢出结论：未发现页面级非预期横向溢出；食物分类条为有意的局部横向滚动。
- 后续非阻塞优化：在真实 iOS/Android 设备补做软键盘、刘海/圆角安全区和系统字体放大视觉验收；动画、主题与细节视觉统一留待后续版本。

## 发布前版本一致性检查

- 版本号：`frontend/package.json` 与 lock 根包版本统一为 `0.1.3`。
- 版本配置：`status=development`、`releasedAt=null`，未填写正式上线时间。
- 版本名称：统一为“v0.1.3 — 基础记录闭环与稳定性收尾”。
- 网页版本记录：最新记录为 v0.1.3，仅展示当前版本内容、概述、重点与分类明细；页面不展示版本状态，未发布上线时间显示“尚未正式上线”。
- 版本文档：`PROJECT_STATUS.md`、`VERSION_HISTORY.md` 与本文件一致；`CHANGELOG.md` 仍只记录已正式发布的 v0.1.2，未提前新增 v0.1.3 正式发布记录。
- Roadmap Review：统计、模板、AI、离线数据库、图片体系、GI/GL、健身和记账继续属于后续范围，本次未改写 Roadmap。
- 部署信息：未创建部署地址、部署 ID 或 tag，未声明 Production 已更新。

## 修改意见与建议 Review

- 已完成能力：修改意见提交、历史查看、编辑、删除、管理员完成/重开、完成时间与完成版本关联；对应能力已在 v0.1.2 完成。
- v0.1.3 仓库记录 Review：与基础闭环相关的首页删除、退出账号、日期规则、状态恢复和移动端问题均已完成并纳入本版本。
- 明确延期到后续版本：真实设备软键盘、安全区、系统字体放大、动画和视觉细节；统计、模板、AI、离线数据库、图片体系、GI/GL、健身和记账模块。
- Production pending 状态：真实建议存储在 Supabase `version_feedback`；本地仓库无真实业务数据，无法确认线上 pending 列表为空。正式部署前必须由管理员在现有修改意见页面复核，未虚构完成状态。
- Review 结论：未发现仓库文档中被错误标记为 v0.1.3 已完成的后续功能，也未发现已知代码级上线阻塞项。

## 最终发布前验证

- `npm run validate:version`：通过，输出 `Version validation passed for v0.1.3`。
- 全量测试第一次：版本校验通过；23 个套件中 1 个版本页断言失败，其余 163 个测试通过。原因是未发布上线时间显示“未填写”，与既定“尚未正式上线”文案不一致。
- 修复：统一 `formatReleaseTime(null)` 为“尚未正式上线”，同步工具测试。
- 全量测试最终结果：23 个测试套件、164 个测试全部通过。
- `npm run build`：通过，`Compiled successfully`。
- 非阻塞输出：测试环境缺少 Supabase URL/匿名 key 的既有提示、模拟 session 恢复失败的预期错误日志；构建输出 Node `fs.F_OK` 弃用警告。
- 手机端验收：四档 viewport 回归通过；Dialog、AlertDialog、AddFoodSheet、长名称、删除点击区和底部导航均已覆盖。

## 正式上线前剩余步骤

1. 确认工作区干净。
2. 确认全部提交已 push。
3. 确认本地 HEAD 与远程分支一致。
4. 部署最新远程 commit 到 Production。
5. 验证 Production 实际运行版本。
6. 回填真实上线时间和部署信息。
7. 创建并推送 v0.1.3 tag。

本任务只执行第 1—3 步；第 4—7 步未获授权，不得提前执行。

## Production 反馈修复（1/6）

- Feedback ID：`4571c958-7f36-42e2-b8a1-0f697452d18b`
- 标题：删除历史后放回历史记录无返回按钮
- 状态：代码、测试与提交已完成；Production feedback 状态待安全管理权限更新。
- 修复内容：
  - 历史记录列表顶部始终显示返回按钮，提供 `aria-label="返回上一页"` 和至少 44×44px 点击区。
  - 明确从设置进入时返回 `/settings`；具有有效应用内前序记录时优先返回上一页。
  - 直接访问、刷新或从详情指定安全回退时 replace 到 `/settings`。
  - 从列表进入详情时保留设置来源；详情返回或删除后进入列表时保留安全回退语义。
  - 不恢复底部历史入口，不改变删除、批量删除、日期清理或首页日期规则。
- 验证结果：
  - 专项测试：4 个套件、31 个用例全部通过。
  - 全量测试：24 个套件、170 个用例全部通过。
  - Production build：通过（Compiled successfully）。
  - 非阻塞输出：既有 Supabase 测试环境提示、模拟 session 失败日志和 Node `fs.F_OK` 弃用警告。
- 相关 Commit：8949a5bf46a711b9720dee5e69bc28a9978ff7e8
- 本轮进度：1/6；本次不 push、不部署、不创建 tag。

## Production 反馈修复（2/6）

- Feedback ID：`33bd0635-8244-44e7-96bc-a02a98b4d588`
- 标题：时间轴修改时间问题
- 状态：代码与测试已完成；Production feedback 状态待安全管理权限更新。
- 修复内容：
  - 固定三餐继续使用现有 `meal + breakfast/lunch/dinner` 数据结构，不新增第二套餐次模型。
  - 首页固定三餐移除独立“编辑”按钮，用户直接点击当前时间打开修改 Sheet；加餐和其他项目不受影响。
  - Sheet 显示当前 24 小时时间，支持取消、非法输入拦截、保存加载态、防重复提交和失败保留输入。
  - 已落库餐次通过 `id + user_id` 权限条件更新 `event_time`；尚未落库的默认餐次首次修改只创建一条对应类型记录。
  - 数据库成功后才更新页面，保留餐内食物；失败时保留原时间并显示错误。
  - 首页按实际时间稳定重排；当前日期初始化重新读取持久化记录并补齐缺失固定三餐，刷新后恢复修改时间。
  - 历史详情页原有查看和编辑流程保持不变。
- 验证结果：
  - 专项测试：3 个套件、12 个用例全部通过。
  - 全量测试：26 个套件、176 个用例全部通过。
  - Production build：通过（Compiled successfully）。
  - 非阻塞输出：既有 Supabase 测试环境提示、模拟 session 失败日志和 Node `fs.F_OK` 弃用警告。
- 旧数据风险：
  - 如 Production 旧数据已经存在同一用户、日期和固定餐次类型的重复行，本次不执行数据迁移或自动删除；正常修改路径不会新建第二条已落库固定餐次。
- 相关 Commit：8124bc609fa572336d8d647abcce8ba25ec1bc24
- 本轮进度：2/6；本次不 push、不部署、不创建 tag。

## Production 反馈修复（3/6）

- Feedback ID：`d08f38c9-07e9-48ec-a734-6c994672f823`
- 标题：添加修改后提示信息显示过长
- 状态：代码与测试已完成；Production feedback 状态待安全管理权限更新。
- 修复内容：
  - 继续使用项目根级 Sonner `Toaster`，新增统一 `showSuccess` 封装，成功提示显示 2000ms 后自动消失。
  - 相同成功文案使用稳定 ID，连续触发时更新同一 Toast，避免重复堆叠；消失后不留下页面占位。
  - 覆盖首页添加/修改、食物库添加/编辑、餐次时间修改、历史编辑、摄入计划、账户资料、记录设置、密码和修改意见管理等成功反馈。
  - 数据保存、状态更新和导航仍由原请求流程控制，不依赖 Toast 生命周期。
  - 错误 Toast 保持 Sonner 默认时长和手动关闭；页面内校验错误继续保持至用户修正。
  - Dialog、AlertDialog、加载状态和固定页面提示均未改动；首页非本日固定提示留待下一项反馈。
- 验证结果：
  - 专项测试：7 个套件、44 个用例全部通过。
  - 全量测试：27 个套件、180 个用例全部通过。
  - Production build：通过（Compiled successfully）。
  - fake timers 覆盖 2 秒自动消失、无占位、同文案去重、卸载清理和错误提示不被短时关闭。
  - 非阻塞输出：既有 Supabase 测试环境提示、模拟 session 失败日志和 Node `fs.F_OK` 弃用警告。
- 相关 Commit：a66dd0e91d5b11ec04e95ef5bbc10544a505f043
- 本轮进度：3/6；本次不 push、不部署、不创建 tag。

## 新增上线前需求（4/6）

- 标题：所有修改时间界面增加“使用现在时间”
- Feedback ID：当前环境无法安全查询 Production，未确认存在对应 pending 记录，未伪造 ID。
- 状态：代码、测试与 build 已完成。
- 适用范围：
  - 共享 `EditTimeSheet`：首页固定早餐/午餐/晚餐、首页加餐、历史详情餐次时间。
  - `EditActivitySheet`：首页及历史详情的训练与其他事件，分别处理开始时间和结束时间。
- 实际行为：
  - 点击按钮使用设备当前本地小时与分钟，补零为 `HH:mm`；不包含秒，不进行 UTC 转换。
  - 只填入本地表单状态，不调用数据库、不自动保存、不关闭 Sheet。
  - 填入后可以继续手动修改；取消不改变原记录；点击原保存按钮才进入既有保存流程。
  - 开始与结束时间各有独立按钮，单次点击不会同时覆盖两个字段。
  - 保存失败继续保留输入；成功后沿用自动消失的成功提示。
  - 进行中记录仍禁止修改开始/结束时间；新建记录和自动归档设置不属于本次范围。
- 移动端：
  - 按钮使用全宽、至少 44px 高的次要按钮布局，在 320px 宽度下与输入、取消和保存纵向排列，不制造横向溢出。
- 验证结果：
  - 专项测试：5 个套件、37 个用例全部通过。
  - 全量测试：28 个套件、185 个用例全部通过。
  - Production build：通过（Compiled successfully）。
  - 固定系统时间 `2026-07-28 09:05` 预期并实际填入 `09:05`；fake timers 均恢复。
  - 非阻塞输出：既有 Supabase 测试环境提示、模拟 session 失败日志和 Node `fs.F_OK` 弃用警告。
- 相关 Commit：4986525fef380e81cc68e6ca9c1df05ddea4e3b6
- 当前进度：4/6；本次不 push、不部署、不创建 tag。

## Production 反馈修复（5/6）

- Feedback ID：`ec401249-78d3-4c3c-b0fc-6dfae568cd89`
- 标题：点击其他页需要有提示
- 实际确认需求：首页查看或修改非真实本日日期时显示固定日期状态提示。
- 状态：代码、测试与 build 已完成；Production feedback 状态待安全管理权限更新。
- 显示规则：
  - 首页当前查看日期与设备本地真实日期不同即显示，返回真实本日立即消失。
  - 查看过去和未来日期均显示；结束本日自动进入下一记录日后显示。
  - 刷新或重新登录恢复到非本日记录日后继续显示。
  - 删除真实本日历史并恢复真实本日后不显示；日期状态清理继续由既有 Store 逻辑负责。
- 文案与格式：
  - 同年示例：“你已离开本日，当前正在查看和修改 7月29日的记录与计划。”
  - 跨年示例：“你已离开本日，当前正在查看和修改 2027年1月2日的记录与计划。”
  - 使用 `getLocalDateKey` 读取设备本地年月日，只比较日期，不使用 UTC ISO 字符串。
- 界面：
  - 卡片位于首页同步状态之后、日期标题之前，随页面正常滚动，不遮挡日期切换、汇总或添加操作。
  - `max-w-full` 与自动换行保证 320px 宽度下不制造页面级横向溢出。
  - 不触发 Toast、Dialog，不使用自动消失计时器。
- 验证结果：
  - 专项测试：4 个套件、41 个用例全部通过。
  - 全量测试：29 个套件、189 个用例全部通过。
  - Production build：通过（Compiled successfully）。
  - 覆盖本日、过去、未来、切换同步、返回本日、自动推进、刷新恢复、本地午夜、跨年、固定显示和移动端回归。
  - 非阻塞输出：既有 Supabase 测试环境提示、模拟 session 失败日志和 Node `fs.F_OK` 弃用警告。
- 相关 Commit：c1d8e2c4f449a3f9d283c5e5dd7073a9cbec1037
- 当前进度：5/6；本次不 push、不部署、不创建 tag。

## 新增上线前需求（6/9）

- 标题：建议历史分区、排序、只读权限和提交天数显示
- Feedback ID：当前环境无法安全查询 Production，未确认存在对应 pending 记录，未伪造 ID。
- 状态：代码、测试、migration 与 build 已完成；migration 尚未部署。
- 页面分区与排序：
  - “未完成建议”位于上方，只显示 pending，按 `created_at` 倒序。
  - “已完成建议”位于下方，只显示 completed，按 `completed_at` 倒序，并以 `updated_at`、`created_at` 和 ID 作为稳定次级排序。
  - 两个区域分别显示数量和空状态，完成后的卡片不会重复出现在两个区域。
- 提交天数：
  - pending 卡片显示“已提交 X 天”；以设备本地年月日计算自然日差，不按滚动 24 小时计算，也不使用 UTC ISO 日期判断本日。
  - 当天为 0 天，跨一天为 1 天，异常未来时间最低为 0 天。
- 已完成只读：
  - 普通用户和管理员页面均不显示 completed 的编辑、删除、保存、撤销或恢复入口，点击卡片不会进入编辑态。
  - 管理员将 pending 标记完成成功后，该项立即移动到已完成区域顶部，显示完成版本与完成时间；失败时仍留在未完成区域。
  - Service 更新和删除均附加 `status = pending` 条件，避免页面绕过。
  - 新增 `021_lock_completed_version_feedback.sql`：更新触发器保护已完成内容，删除触发器拒绝常规应用调用删除 completed，owner UPDATE/DELETE RLS 仅允许 pending；管理员仍可通过既有受控 RPC 执行必要状态管理。
- 验证结果：
  - 专项测试：前端 2 个套件、20 个用例通过；migration 静态契约 3 个用例通过。
  - 全量测试：30 个套件、197 个用例全部通过。
  - Production build：通过（Compiled successfully）。
  - 非阻塞输出：既有 Supabase 测试环境变量提示、模拟 session 恢复失败日志和 Node `fs.F_OK` 弃用警告。
- 风险与后续：
  - 当前会话未执行远程 migration，需在正式发布前由管理员在目标 Supabase 环境应用并验证 RLS/触发器。
  - 未安全访问 Production `version_feedback`，无法确认是否有对应 Feedback ID，也未修改任何线上反馈状态。
- 相关 Commit：892eaacdfff960eb699510949b82c9cae1dd244c
- 当前进度：6/9；本次不 push、不部署、不创建 tag。

## 新增上线前需求（7/9，已由后续修正替代）

- 标题：退出账号入口移动到设置页最底部
- Feedback ID：没有已确认的独立 Production Feedback ID，未伪造记录。
- 状态：代码、测试与 build 已完成。
- 入口位置：
  - 从顶部“账号”分组移除退出入口，不保留重复项。
  - 新入口位于账户、个人信息、版本信息、记录相关设置和底部版本链接之后，是设置页最后一个可操作设置项。
  - 不加入底部导航，不使用悬浮或固定定位。
- 移动端与误触保护：
  - 独立退出区域与前一内容使用明确垂直间距，继续沿用现有设置项视觉结构和触摸区域。
  - 页面底部调整为 `pb-32`，并叠加 `env(safe-area-inset-bottom)`，保证 320px 宽度和带安全区设备上不被底部导航遮挡。
- 退出行为：
  - 确认弹窗、取消保持登录、确认调用既有 logout、防重复、错误提示、私有查询清理及 replace 到普通登录页全部保持原实现。
  - Store 清理、公共食品数据、认证初始化和受保护路由未修改。
- 验证结果：
  - 专项测试：5 个套件、38 个用例全部通过。
  - 全量测试：30 个套件、200 个用例全部通过。
  - Production build：通过（Compiled successfully）。
  - 非阻塞输出：既有 Supabase 测试环境变量提示、模拟 session 恢复失败日志和 Node `fs.F_OK` 弃用警告。
- 相关 Commit：9219280c7458d837c4f89982c0d9f38515b82ceb
- 当前进度：7/9；本次不 push、不部署、不创建 tag，migration 021 未部署。

## 新增上线前需求（8/9）

- 标题：统一修改意见相关子页面返回入口
- Feedback ID：没有已确认的独立 Production Feedback ID，未伪造记录。
- 状态：代码、测试与 build 已完成。
- 页面与路由核对：
  - 版本信息正式路由为 `/settings/version`。
  - 修改意见模块只有 `/settings/version/feedback` 一个实际子页面；提交建议、建议历史和管理员处理使用该页 Tabs，没有额外详情、编辑或管理路由。
- 返回行为：
  - 反馈页共享顶部按钮从“返回设置”改为“返回版本信息”，`aria-label` 同步为“返回版本信息”。
  - 点击明确 replace 到 `/settings/version`，不使用 `navigate(-1)`；正常进入、直接访问和刷新后行为一致，且不会形成返回循环。
  - 页面正文原有第二个“返回版本信息”链接已删除，最终只保留一个顶部入口，不留下多余容器。
  - 共享 Header 保留默认“返回设置”行为，其他普通设置子页不受影响；版本信息页自身没有“返回版本信息”循环入口。
- 功能隔离：
  - 反馈提交、历史加载、pending/completed 分区、排序、completed 只读、编辑删除和管理员完成逻辑未改变。
  - Service、认证、权限与 migration 021 均未修改。
- 验证结果：
  - 专项测试：6 个套件、54 个用例全部通过。
  - 全量测试：31 个套件、204 个用例全部通过。
  - Production build：通过（Compiled successfully）。
  - 非阻塞输出：既有 Supabase 测试环境变量提示、模拟 session 恢复失败日志和 Node `fs.F_OK` 弃用警告。
- 相关 Commit：6f5a935c577f9e0853cf7cdfa2d03961674ae497
- 当前进度：8/9；本次不 push、不部署、不创建 tag，migration 021 未部署。

## 新增上线前修正（9/10）

- 标题：恢复退出账号在 v0.1.2 中的位置
- Feedback ID：没有已确认的独立 Production Feedback ID，未伪造记录。
- 状态：代码、测试与 build 已完成。
- 历史依据：
  - 从 Git tag `v0.1.2` 读取 `SettingsPage.jsx`，实际顺序为账号分组、记录分组、账号操作分组（退出账号）、底部版本号链接。
  - 因此前一项“移动到整个页面最底部”的位置要求已被本修正替代，不再代表当前有效布局。
- 当前布局：
  - 唯一“退出账号”恢复到独立“账号操作”分组，紧跟记录分组。
  - 底部版本号链接位于退出账号之后，退出入口不是页面最后一个元素。
  - 不显示“切换账号”；保留 `pb-32` 和安全区底部留白，320px 下入口完整可点击。
- 功能隔离：
  - 确认、取消、防重复、失败提示、私有 Store 清理、公共食品保留、replace 登录页及受保护路由规则均未修改。
- 验证结果：
  - 专项测试：5 个套件、38 个用例全部通过。
  - 全量测试：31 个套件、204 个用例全部通过。
  - Production build：通过（Compiled successfully）。
  - 非阻塞输出：既有 Supabase 测试环境变量提示、模拟 session 恢复失败日志和 Node `fs.F_OK` 弃用警告。
- 相关 Commit：30092cdc5becc70a906fe99b2915d6e31c5d6d3e
- 当前总进度：9/10；本次不 push、不部署、不创建 tag，migration 021 未部署。

## 最后一项上线前需求（10/10）

- 标题：优化进入建议历史页面的响应速度
- Feedback ID：没有已确认的独立 Production Feedback ID，未伪造记录。
- 状态：代码、测试与 build 已完成；全部上线前任务完成，等待统一 push。
- 真实性能原因：
  - 页面历史数据只存在组件本地 state，离开再返回会无条件重新请求，无法复用已加载结果。
  - 提交成功时同时切换历史 Tab 并显式调用 `loadHistory`，旧 effect 也会加载，存在两次等价调用路径。
  - 旧请求没有页面卸载和账号切换身份保护，迟到结果可能写入已经变化的页面上下文。
  - 无缓存时加载期间只显示一行提示，未完成/已完成区域结构要等请求结束后才出现。
- 优化：
  - 复用项目现有 React Query，不新增依赖；按 `private/version-feedback/userId/role` 建立用户与权限隔离的缓存 key。
  - 新鲜缓存有效期为 60 秒：返回页面时同步显示并不再发送请求；过期缓存先显示，再由单次后台请求替换。
  - `fetchQuery` 复用同 key in-flight Promise，Tab handler、effect 与 React Strict Mode 重复生命周期不会产生两次等价网络请求。
  - 无缓存时立即显示页面标题、唯一返回入口、Tabs、两个分区标题和“正在加载建议历史”；失败后结束 loading，显示错误与可用重试。
  - 请求结果写入前检查组件挂载状态和当前用户/权限身份；卸载或账号切换后的旧结果不更新当前页面。
  - 反馈编辑、删除、管理员完成和分页结果同步回缓存，避免返回页面显示旧卡片或重复卡片。
  - 退出账号的 private query 清理 predicate 已覆盖反馈缓存；不同账号 key 完全隔离。
  - Supabase 列表查询由 `select('*')` 收紧为页面实际需要字段，保留 status、created/completed/updated 排序字段；仍使用一次反馈查询后前端分区，没有按状态拆分查询。
- 验证结果：
  - 专项测试：`VersionFeedbackPage` 与 `SettingsPage` 共 2 个套件、34 个用例通过。
  - 全量测试：31 个套件、210 个用例全部通过。
  - Production build：通过（Compiled successfully）。
  - 覆盖即时结构/loading、成功分区、单次请求、Strict Mode、缓存返回、过期刷新、去重、失败重试、卸载迟到响应、跨账号隔离、退出缓存清理及既有业务规则。
  - 非阻塞输出：既有 Supabase 测试环境变量提示、模拟 session 恢复失败日志和 Node `fs.F_OK` 弃用警告。
- 数据库：未新增 migration，`021_lock_completed_version_feedback.sql` 仍未部署。
- 相关 Commit：cf169e397fb18998cb0481ab1859e276dace2f85
- 当前总进度：10/10；本任务提交与回填后统一 push，Production 仍未部署。
