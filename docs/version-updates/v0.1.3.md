# v0.1.3

## 版本名称

v0.1.3 — 基础记录闭环与稳定性收尾

## 版本状态

development（上线前检查完成，待发布）

## 上线时间

待正式上线确认（status=development，releasedAt=null）

## 版本目标

- 完成 v0.1 阶段核心记录链路的最终收尾。
- 以修复缺口和补齐验证为主，不扩展新模块。

## 纳入范围

- 登录与退出账号。
- 首页饮食记录新增、编辑、删除。
- 删除全部食物后自动删除空餐。
- 结束本日和自动进入下一记录日。
- 首页真实日期、当前记录日和已结束状态规则。
- 删除本日历史记录后首页返回真实本日。
- 历史记录查看、编辑、单条删除和批量删除。
- 设置入口进入历史记录。
- 底部导航只保留首页、食物库、设置。
- 食物库基础功能和用户数据权限。
- 基础计划功能。
- 刷新、重新登录后的数据与页面状态。
- 手机端主要页面。
- Production 是否为最新部署版本。

## 明确不纳入

- 统计功能扩展。
- 模板。
- AI。
- 离线数据库。
- 图片体系扩展。
- GI/GL 扩展。
- 健身模块扩展。
- 记账模块。

## 当前完成情况（代码与测试证据核对）

### 已完成，可直接验收

- 登录、退出、会话恢复主链路已实现。
- 设置页账号区域的退出账号闭环已完成；退出成功后清理私有运行时状态并返回普通登录页。
- “结束本日／删除本日历史／返回首页”关键路径回归已完成，覆盖日期推进、完成状态、删除隔离、取消/失败和重新初始化。
- 刷新与重新登录状态一致性回归已完成，覆盖 session 恢复等待、无 session 降级、日期恢复、同账号数据重载、跨账号隔离、失败状态与重复认证回调。
- 已修复切换账号时旧 profile 短暂保留，以及旧账号 history/plan 异步响应迟到覆盖新账号数据的竞态。
- 手机端主要页面验收已完成，覆盖 320×568、375×667、390×844、430×932 四档 viewport。
- 已修复添加食物 Sheet 在矮屏的滚动与动态高度、通用弹窗超出动态视口、长食物名称挤压操作区和首页删除入口点击面积偏小的问题。
- 首页当日食物删除与空餐自动清理已实现；删除成功后会立即更新餐次及当日热量、蛋白质、脂肪和碳水汇总。
- 结束本日推进与首页日期动态规则已实现。
- 删除本日历史后首页回到真实本日已实现。
- 历史记录查看、编辑、单条删除、批量删除已实现。
- 设置入口进入历史记录已实现。
- 底部导航三入口已实现。
- 食物库基础能力与权限主链路已实现。
- 基础计划功能已实现。

### 已实现但需要修复或补充测试

- 无仓库内已知上线阻塞项；Production 当前仍是 v0.1.2，正式部署与线上验收尚未执行。

### 尚未实现，需要纳入 v0.1.3

- 无；本地发布前检查已完成。

### 不属于 v0.1.3，移入后续版本

- 统计增强。
- 模板。
- AI。
- 离线数据库与同步。
- 图片体系扩展。
- GI/GL 扩展。
- 健身模块扩展。
- 记账模块。

## 待处理任务清单（按优先级）

### P0

- 已完成：退出账号、首页食物删除、结束本日／删除本日历史／返回首页关键路径均有自动化回归。

### P1

- 已完成：刷新、重新登录后的状态一致性回归。
- 已完成：手机端主要页面验收记录。

### P2

- 已完成：Production 版本一致性核对流程与发布前检查清单。

## 上线验收标准

- P0 与 P1 全部完成并有测试或人工验收记录。
- 版本文档、版本索引、项目状态文档一致。
- 完成生产环境版本一致性核对。
- 仅在正式上线后回填上线时间与 releasedAt。

## Commit ID 回填位置

- 发布范围确认主提交：6dd2e5d。
- 文档回填提交：待回填。

## 正式上线时间回填位置

- 本文件“上线时间”章节。
- frontend/src/config/version.config.json 的 releasedAt 字段（仅正式上线后填写）。

## 相关提交

- 范围确认提交：6dd2e5d
- 首页当日食物删除与空餐清理：deb10ece832d4f4dcee6bf34a78c486f03d96546
- 退出账号功能：746daeb04c7e390588f13d6984a9700f823eea41
- 结束本日与历史删除回归：f132b6c3c9aa3f54454c8a54b464587c01e291f9
- 刷新与重新登录状态一致性：81860a472efdb3b048ef1eba2c8d6c03a93b59c0
- 手机端主要页面验收：a0eccfbc602ff09c5b2e7f034dead1026b278749
- 发布前版本一致性与修改建议 Review：fcf5f6dcc5199992ed38d16bf20962b050cc20ef
- 回填提交：待回填

## 手机端主要页面验收记录

- 验收 viewport：320×568、375×667、390×844、430×932。
- 验收方式：固定 viewport 的组件渲染与交互回归、现有页面测试、响应式结构与公共样式审查、生产构建；未连接生产数据库，未冒充真实设备人工测试。
- 登录页：通过。输入框、登录按钮和错误区域结构完整，不存在“切换账号”或专用切换提示。
- 首页：已修复后通过。日期、营养卡片和时间轴保留底部空间；长食物名称可换行，删除按钮扩大为 40×40px 并保持可点击；删除确认操作完整。
- 添加食物 Sheet：已修复后通过。使用动态视口高度与最大高度，列表及选中食物后的克重/确认区域可滚动，超长名称不挤压热量与操作区。
- 历史记录列表：通过。日期、批量删除入口和空状态结构正常，页面使用 `pb-32` 避免底部导航覆盖。
- 历史详情：已修复后通过。查看、编辑、删除入口可操作；长内容沿用时间轴换行规则；公共确认弹窗具备动态最大高度与内部滚动。
- 食物库：已修复后通过。分类条使用自身横向滚动，不造成页面横向溢出；超长食物名称与营养文字可换行；添加/编辑表单通过通用 Dialog 的动态高度和滚动保护保持可提交。
- 计划页面：通过。热量单行与三列宏量输入均使用可收缩宽度，结果和保存区域保留底部空间。
- 设置页面：通过。设置项采用可收缩文本区与固定图标；历史记录和退出账号入口可点击，不显示切换账号。
- 退出账号确认：已修复后通过。取消和退出操作完整，弹窗不会超过动态视口且内容可滚动。
- 底部导航：通过。仅首页、食物库、设置三个等宽入口，具有安全区处理；主要页面均预留底部空间，不存在历史记录入口。
- 横向溢出结论：未发现页面级非预期横向溢出；食物分类条为有意的局部横向滚动。
- 后续非阻塞优化：在真实 iOS/Android 设备补做软键盘、刘海/圆角安全区和系统字体放大视觉验收；动画、主题与细节视觉统一留待后续版本。

## 发布前版本一致性检查

- 版本号：`frontend/package.json` 与 lock 根包版本统一为 `0.1.3`。
- 版本配置：`status=development`、`releasedAt=null`，未填写正式上线时间。
- 版本名称：统一为“v0.1.3 — 基础记录闭环与稳定性收尾”。
- 网页版本记录：最新记录为 v0.1.3，仅展示当前版本内容、概述、重点与分类明细；页面不展示版本状态，未发布上线时间显示“尚未正式上线”。
- 版本文档：`PROJECT_STATUS.md`、`VERSION_HISTORY.md` 与本文件一致；`CHANGELOG.md` 仍只记录已正式发布的 v0.1.2，未提前新增 v0.1.3 正式发布记录。
- Roadmap Review：统计、模板、AI、离线数据库、图片体系、GI/GL、健身和记账继续属于后续范围，本次未改写 Roadmap。
- 部署信息：未创建部署地址、部署 ID 或 tag，未声明 Production 已更新。

## 修改意见与建议 Review

- 已完成能力：修改意见提交、历史查看、编辑、删除、管理员完成/重开、完成时间与完成版本关联；对应能力已在 v0.1.2 完成。
- v0.1.3 仓库记录 Review：与基础闭环相关的首页删除、退出账号、日期规则、状态恢复和移动端问题均已完成并纳入本版本。
- 明确延期到后续版本：真实设备软键盘、安全区、系统字体放大、动画和视觉细节；统计、模板、AI、离线数据库、图片体系、GI/GL、健身和记账模块。
- Production pending 状态：真实建议存储在 Supabase `version_feedback`；本地仓库无真实业务数据，无法确认线上 pending 列表为空。正式部署前必须由管理员在现有修改意见页面复核，未虚构完成状态。
- Review 结论：未发现仓库文档中被错误标记为 v0.1.3 已完成的后续功能，也未发现已知代码级上线阻塞项。

## 最终发布前验证

- `npm run validate:version`：通过，输出 `Version validation passed for v0.1.3`。
- 全量测试第一次：版本校验通过；23 个套件中 1 个版本页断言失败，其余 163 个测试通过。原因是未发布上线时间显示“未填写”，与既定“尚未正式上线”文案不一致。
- 修复：统一 `formatReleaseTime(null)` 为“尚未正式上线”，同步工具测试。
- 全量测试最终结果：23 个测试套件、164 个测试全部通过。
- `npm run build`：通过，`Compiled successfully`。
- 非阻塞输出：测试环境缺少 Supabase URL/匿名 key 的既有提示、模拟 session 恢复失败的预期错误日志；构建输出 Node `fs.F_OK` 弃用警告。
- 手机端验收：四档 viewport 回归通过；Dialog、AlertDialog、AddFoodSheet、长名称、删除点击区和底部导航均已覆盖。

## 正式上线前剩余步骤

1. 确认工作区干净。
2. 确认全部提交已 push。
3. 确认本地 HEAD 与远程分支一致。
4. 部署最新远程 commit 到 Production。
5. 验证 Production 实际运行版本。
6. 回填真实上线时间和部署信息。
7. 创建并推送 v0.1.3 tag。

本任务只执行第 1—3 步；第 4—7 步未获授权，不得提前执行。

## Production 反馈修复（1/4）

- Feedback ID：`4571c958-7f36-42e2-b8a1-0f697452d18b`
- 标题：删除历史后放回历史记录无返回按钮
- 状态：代码、测试与提交已完成；Production feedback 状态待安全管理权限更新。
- 修复内容：
  - 历史记录列表顶部始终显示返回按钮，提供 `aria-label="返回上一页"` 和至少 44×44px 点击区。
  - 明确从设置进入时返回 `/settings`；具有有效应用内前序记录时优先返回上一页。
  - 直接访问、刷新或从详情指定安全回退时 replace 到 `/settings`。
  - 从列表进入详情时保留设置来源；详情返回或删除后进入列表时保留安全回退语义。
  - 不恢复底部历史入口，不改变删除、批量删除、日期清理或首页日期规则。
- 验证结果：
  - 专项测试：4 个套件、31 个用例全部通过。
  - 全量测试：24 个套件、170 个用例全部通过。
  - Production build：通过（Compiled successfully）。
  - 非阻塞输出：既有 Supabase 测试环境提示、模拟 session 失败日志和 Node `fs.F_OK` 弃用警告。
- 相关 Commit：待回填
- 本轮进度：1/4；本次不 push、不部署、不创建 tag。
