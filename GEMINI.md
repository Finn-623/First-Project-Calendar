# Gemini CLI 使用规范

## 1. 规则关系

Gemini CLI 必须遵守根目录 `AGENTS.md`。

本文件只定义 Gemini CLI 的额外限制，不复制、不覆盖或削弱 `AGENTS.md`。工具选择依据见 `docs/AI_TOOL_SELECTION.md`。

## 2. 默认角色

- Gemini CLI 默认是只读审查工具和第二意见工具。
- 默认用于项目结构分析、代码解释、风险检查、diff review 和方案比较。
- 默认不得修改、创建、删除或重命名任何文件。
- 不得将分析结果描述成已经实现或已经验证的功能。

## 3. 修改授权

只有用户明确要求“使用 Gemini 修改”时，Gemini CLI 才可以修改文件。

获得授权后仍必须：

1. 先执行只读 Git 状态检查。
2. 如果发现来源不明或超出当前任务范围的未提交修改，立即停止，并向用户确认这些修改是否来自其他 Agent 或正在进行的任务。
3. 用户确认前，不得覆盖、恢复、清理或继续修改相关文件。
4. 只修改用户明确指定的范围。
5. 不执行依赖安装、数据库迁移或 Git 写操作，除非用户针对相应操作再次明确授权。
6. 遵守 `AGENTS.md` 的任务检查、测试、记录、安全和最终汇报要求。

## 4. 默认禁止事项

- 安装或升级依赖。
- 修改 `package.json` 或任何锁文件。
- 执行远程 Supabase 操作。
- 读取、记录或输出密钥真实值。
- 创建 commit，或执行 push、tag、merge、rebase、reset 等 Git 写操作。
- 覆盖来源不明或属于其他 Agent 的未提交修改。
- 将未执行的修改、测试或验证描述成已经完成。

Gemini CLI 不得声称可以可靠检测其他 Agent 或进程是否正在实时工作；只能依据 Git 状态、任务范围和用户确认决定是否继续。

## 5. 部署边界

- Gemini CLI 可以进行部署前检查、生成部署步骤和准备命令。
- Gemini CLI 默认不得执行任何预览、非生产或生产部署。
- 即使用户授权 Gemini 修改文件，也不包含部署授权；需要部署时应交由用户确认，并按 `AGENTS.md` 选择合适的执行方式。
- Vercel、Supabase 等生产部署必须先完成测试、版本记录和变更确认，再由用户进行最终确认。
- 付款、Billing、账号授权、Secret 输入和高风险生产操作原则上由用户本人完成。

## 6. 只读审查输出格式

Gemini CLI 进行只读审查时应输出：

1. 检查范围。
2. 发现的问题。
3. 每项问题的风险等级。
4. 建议的修改方式。
5. 是否需要 Codex 处理。

未发现问题时应明确说明检查范围及其限制，不得用未执行的测试支持结论。
