# 项目开发规范与 AI 协作指南

`AGENTS.md` 是本项目最高级的项目开发与 AI 协作规则，同时覆盖开发记录、版本管理、测试、Git、安全和多工具协作。工具专属规则只能增加限制，不能覆盖或削弱本文件。

## 1. 基本原则
每次完成实际代码修改后，必须检查是否需要同步更新记录文档。

所有记录必须基于真实发生的修改、测试和结果。

禁止记录：

- 尚未完成的功能。
- 没有实际执行的测试。
- 不存在的 Commit ID。
- 未经用户确认的版本号。
- 推测出来的历史修改。
- 未经用户确认的规划内容。

不要每次机械修改全部文档，只更新与本次任务直接相关的文件。

---

## 2. 开发任务编号
每次新的代码修改任务使用唯一开发编号。

格式：

```
DEV-YYYYMMDD-序号
```

示例：

```
DEV-20260726-001
DEV-20260726-002
```

规则：

- 同一天从 `001` 开始递增。
- 不得重复已有编号。
- 一个完整开发任务通常对应一个 DEV 编号。
- 一个任务修改多个文件时，不需要为每个文件单独编号。
- 只查看、分析或解释代码，没有实际修改时，可以不创建编号。
- 不得为过去的任务自动补编号。

---

## 3. DEVELOPMENT_LOG.md
路径：

```
docs/DEVELOPMENT_LOG.md
```

用途：

记录每次实际代码修改和开发任务。

每次完成代码修改后，原则上都需要更新。

`DEVELOPMENT_LOG.md` 记录该开发任务的总体测试结论。涉及数据库变更时，可引用对应数据库变更记录，无需在两处复制完全相同的测试内容。

每条记录应包含：

```
开发任务编号
日期
任务状态
任务目标
实际完成内容
主要修改文件或模块
遇到的问题
解决方式
执行的测试
测试结果
未完成事项
风险或注意事项
Git Commit ID
```

Commit ID规则：

- 尚未提交时填写“未提交”。
- 已经提交时填写真实 Commit ID。
- 不得提前编造 Commit ID。
- 后续完成提交后，可以补充真实 Commit ID。

---

## 4. CHANGELOG.md
路径：

```
CHANGELOG.md
```

用途：

记录正式版本变化，不记录所有日常代码修改。

仅在以下情况更新：

- 用户明确要求发布新版本。
- 用户明确确认一个版本节点。
- 用户明确要求整理版本更新内容。

记录内容可包括：

```
版本号
日期
新增
修改
修复
数据库变化
相关 Commit
相关 Git Tag
```

限制：

- 不要自动提高版本号。
- 不要自行决定发布版本。
- 不要自动创建 Git Tag。
- 不得虚构版本号、发布日期、Commit ID 或 Tag。

---

## 5. DATABASE_CHANGES.md
路径：

```
docs/DATABASE_CHANGES.md
```

仅在发生以下变化时更新：

- 新建或删除数据表。
- 新增、删除或修改字段。
- 修改字段类型。
- 修改默认值或约束。
- 修改索引或外键。
- 修改 RLS。
- 修改数据库函数。
- 创建或修改 Migration。
- 修改 Storage 配置。
- 修改本地数据库结构。
- 修改同步、备份或恢复相关结构。

数据库变更编号格式：

```
DB-YYYYMMDD-序号
```

每条记录应包含：

```
数据库变更编号
日期
修改原因
实际修改内容
涉及的表和字段
Migration 文件路径
对现有数据的影响
风险
回滚方式
测试内容
测试结果
相关 DEV 编号
相关 Commit ID
```

`DATABASE_CHANGES.md` 只记录数据库结构、迁移、RLS、权限和数据影响相关的验证结果。它可以与 `DEVELOPMENT_LOG.md` 引用同一个 DEV 编号；两处记录的结论必须一致，但不要求复制完整相同的测试内容。

如果本次任务没有数据库变化，不要修改该文件。

---

## 6. DECISIONS.md
路径：

```
docs/DECISIONS.md
```

用途：

记录重要技术或产品决策。

决策编号格式：

```
ADR-001
ADR-002
```

仅在发生重要决策时更新，例如：

- 更换核心技术方案。
- 调整整体代码架构。
- 改变数据访问方式。
- 改变数据库设计原则。
- 改变权限设计。
- 改变存储方案。
- 引入重要核心依赖。
- 放弃已经采用的重要方案。

每条记录应包含：

```
决策编号
日期
状态
背景
最终决定
选择原因
考虑过的其他方案
对当前代码的影响
对后续开发的影响
```

普通样式修改、文案调整和小型 Bug 修复不需要记录。

---

## 7. ROADMAP.md
路径：

```
docs/ROADMAP.md
```

本文件用于后续记录整体规划。

当前阶段规则：

- 保持为空。
- 不读取代码后自动生成规划。
- 不填写功能列表。
- 不标记任何完成状态。
- 不根据现有页面或文件推测功能是否完成。
- 只有用户明确要求整理规划时，才能写入。

---

## 8. 测试记录规则
每次代码修改后，应执行与修改范围相符的测试。

只能记录实际执行过的测试。

每条测试记录必须说明：

```
执行的命令或检查
测试范围
测试结果
失败信息
是否完成修复
仍然存在的风险
```

规则：

- 不得写“测试通过”，除非测试实际执行并通过。
- 无法执行测试时，应写明原因。
- 测试失败时，应保留真实失败结果。
- 不得删除、跳过或修改测试，只为了让结果显示通过。
- 测试结果分别按第 3 节和第 5 节的职责边界记录；涉及数据库变更时，两处结论必须一致。
- 未修改代码时，不要求执行完整项目测试。

---

## 9. Git规则
未经用户明确要求，不要执行：

```
git commit
git push
git tag
git reset
git revert
git checkout
git switch
git rebase
git merge
```

可以使用以下只读命令检查状态：

```
git status
git diff
git log
git branch
```

规则：

- Git保存真实代码修改。
- 文档解释每次修改的原因和结果。
- 不得修改或重写已有Git历史。
- 不得自动提交。
- 不得自动上传。
- 不得自动切换分支。
- 不得自动创建版本Tag。

---

## 10. 安全规则
不得在任何记录文件中写入：

- 密码。
- Access Token。
- API Key。
- Service Role Key。
- 数据库密码。
- `.env` 的真实内容。
- 私密连接字符串。
- 用户认证信息。
- 用户私人数据。

可以记录“调整了环境变量配置”，但不得记录具体密钥值。

---

## 11. 每次任务结束时的判断规则
按照以下逻辑判断文档更新：

```
实际修改代码
→ 更新 DEVELOPMENT_LOG.md

用户明确发布版本
→ 更新 CHANGELOG.md

数据库结构或权限变化
→ 更新 DATABASE_CHANGES.md

发生重要技术决策
→ 更新 DECISIONS.md

用户明确要求整理规划
→ 更新 ROADMAP.md
```

只更新实际涉及的文档。

---

## 12. 当前初始化状态
当前阶段：

- 只建立记录规则。
- 所有记录从后续新的代码修改任务开始。
- 不补写历史记录。
- 不生成当前版本号。
- 不生成当前规划。
- 不自动分析 Git 历史并生成文档。
- 不自动填写任何功能完成状态。

---

## 13. 文件定位与规则优先级

本文件是 First-Project-Calendar 项目所有 AI 开发工作的最高项目级规则文件。

规则冲突时，按以下顺序执行：

1. 用户当前明确指令。
2. `AGENTS.md`。
3. 当前任务指令。
4. `GEMINI.md` 等工具专属规则。
5. `docs/AI_TOOL_SELECTION.md`。
6. `README.md` 和其他说明文档。

工具专属规则只能增加限制，不得降低本文件的安全、授权、测试和记录要求。

---

## 14. 项目方向与架构边界

- 项目名称：First-Project-Calendar / 饮食软件。
- 当前开发主线是饮食记录 App。
- 长期将扩展摄入、健身、计划、记账及个人助手模块。
- 后续架构必须保持模块化，并为离线优先、本地数据库、同步、备份与恢复预留兼容性。
- 不得为了单个页面或临时需求破坏统一数据访问层和模块边界。

---

## 15. 任务开始前检查

每个实际修改任务开始前必须：

1. 确认项目根目录。
2. 检查当前 Git 分支。
3. 执行 `git status`。
4. 阅读与当前需求直接相关的代码、测试和文档。
5. 搜索是否已有相同页面、组件、服务、路由、状态、数据库字段或记录文件。
6. 优先复用现有模块，不建立重复实现。
7. 如发现来源不明或超出当前任务范围的未提交修改，立即停止，并向用户确认这些修改是否来自其他 Agent 或正在进行的任务；确认前不得覆盖、恢复、清理或继续修改相关文件。

---

## 16. 修改范围与操作授权

- 只修改实现当前需求所必需的文件。
- 禁止无关重构。
- 禁止顺手安装或升级依赖。
- 禁止擅自修改锁文件。
- 禁止删除不理解的旧代码。
- 禁止未经明确授权修改远程数据库或生产数据。
- 部署前检查、部署步骤和待执行命令可以在任务范围内准备。
- 预览环境或非生产部署仍需当前任务明确授权。
- Vercel、Supabase 等生产环境部署不得因为一般开发授权而自动执行。
- 生产部署必须在完成测试、版本记录和变更确认后，由用户最终确认。
- 只有用户针对本次生产部署单独、明确下达执行指令时，Codex 才可以执行。
- 付款、Billing、账号授权、Secret 输入和高风险生产操作原则上由用户本人完成。
- 禁止 `force push`、`reset --hard` 等破坏性操作。
- 用户没有授权的操作，不得以“完成任务”为理由扩大执行范围。

---

## 17. 包管理与标准验证命令

当前项目根目录没有 `package.json`，前端命令必须在 `frontend/` 目录中运行。

当前本机统一使用：

```bash
npm run build
CI=true npm test -- --watchAll=false
```

当前项目没有 lint script，因此：

- 不得声称 lint 已通过。
- 不得为了补 lint 而擅自安装依赖。
- 不得自行删除 `yarn.lock`。
- 不得自行创建、删除或更新 `package-lock.json`。
- 不得为了 Yarn 不可用而修改业务代码。

后端测试仅在后端发生修改或当前需求明确影响后端时执行。运行前必须：

1. 检查项目现有的 Python 环境和测试配置。
2. 优先使用项目本地虚拟环境及现有依赖，不假设项目一定存在 `.venv`、Poetry 或 Pipenv。
3. 不得直接向系统或全局 Python 环境安装依赖。
4. 不得擅自创建新的虚拟环境或修改 `requirements.txt`。
5. 如果缺少可用环境，停止并汇报，由用户决定是否配置。

环境可用时，使用项目现有 pytest 配置执行后端测试。

---

## 18. 测试与验证规则

每个实际功能修改必须：

1. 先运行与需求直接相关的测试。
2. 修改可能影响全局行为时，运行完整前端测试。
3. 运行前端生产构建。
4. 修改后端时运行现有后端测试。
5. 修改版本信息时运行项目已有版本校验命令。
6. 区分 warning、error 和 test failure。
7. 未执行的测试不得写成已通过。
8. 无法执行测试时必须说明阻塞原因。

不得删除、跳过或修改测试，只为了得到通过结果。

纯文档修改不强制执行前端构建和测试，但必须检查 diff、文件结构和文档内容一致性。

---

## 19. Supabase 与数据库规则

涉及 Supabase 或数据库时必须：

1. 先检查现有 schema、迁移文件、RLS、调用路径和权限边界。
2. 使用新的迁移文件，不篡改已应用的历史迁移。
3. 说明兼容性和对现有数据的影响。
4. 检查 `user_id`、`visibility`、公共库和个人库权限。
5. 未经明确授权，不执行远程迁移。
6. 不得删除生产数据。
7. 不得输出密钥、Token、用户密码、邮箱和真实 UID。

数据库变更仍须遵守第 5 节的记录要求。

---

## 20. 文档更新边界

只更新与当前任务实际相关的文档，不得为了形式而修改全部记录文件。

常见文档包括：

- `docs/DEVELOPMENT_LOG.md`
- `CHANGELOG.md`
- `docs/DATABASE_CHANGES.md`
- `docs/DECISIONS.md`
- `docs/PROJECT_STATUS.md`
- `docs/VERSION_HISTORY.md`
- `docs/ROADMAP.md`

各文件的具体更新条件以本文件第 3 至第 7 节及第 11 节为准。

---

## 21. Git 与独立提交

- 每个独立修改需求应形成独立 commit，多个无关需求不得混在同一 commit。
- commit 前必须检查 `git diff` 和 `git status`。
- commit 信息必须准确描述本次修改。
- 未经用户明确授权，不执行 commit、push、tag、merge、rebase、reset 或切换分支。
- 只有用户明确要求完成独立提交时，才允许创建 commit。
- 创建 commit 后必须获取真实 Commit ID，并按项目现有记录方式回填。
- 不得虚构 Commit ID。

“应形成独立 commit”表示提交组织原则，不代表 AI 自动获得创建 commit 的授权。

---

## 22. AI 协作规则

- 同一时间只能有一个 AI 工具作为项目文件的主要修改者。
- GitHub Copilot 只用于当前文件补全和小范围、经人工确认的修改。
- 不允许多个 Agent 同时修改相同文件。
- 开始修改前以及切换主要写入工具前，必须检查 `git status`。
- 如果发现来源不明或超出当前任务范围的未提交修改，必须立即停止，并向用户确认这些修改是否来自其他 Agent 或正在进行的任务。
- 在用户确认前，不得覆盖、恢复、清理或继续修改相关文件。
- AI 不得声称已可靠检测其他 Agent 或进程的实时工作状态；协作判断以 Git 状态、任务范围和用户确认为准。

具体工具选择规则见 `docs/AI_TOOL_SELECTION.md`；Gemini CLI 的额外限制见 `GEMINI.md`。

---

## 23. 最终汇报格式

每个任务完成后必须汇报：

1. 实际完成内容。
2. 修改文件。
3. 未完成或明确未修改的内容。
4. 实际执行的测试和结果。
5. 警告或风险。
6. 当前 Git 状态。
7. Commit ID（如已提交；未提交时明确说明）。
8. 是否需要用户进行人工操作。
