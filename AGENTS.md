# 开发记录与版本管理规则

## 1. 基本原则
每次完成实际代码修改后，必须检查是否需要同步更新记录文档。

所有记录必须基于真实发生的修改、测试和结果。

禁止记录：

- 尚未完成的功能。
- 没有实际执行的测试。
- 不存在的 Commit ID。
- 未经用户确认的版本号。
- 推测出来的历史修改。
- 未经用户确认的规划内容。

不要每次机械修改全部文档，只更新与本次任务直接相关的文件。

---

## 2. 开发任务编号
每次新的代码修改任务使用唯一开发编号。

格式：

```
DEV-YYYYMMDD-序号
```

示例：

```
DEV-20260726-001
DEV-20260726-002
```

规则：

- 同一天从 `001` 开始递增。
- 不得重复已有编号。
- 一个完整开发任务通常对应一个 DEV 编号。
- 一个任务修改多个文件时，不需要为每个文件单独编号。
- 只查看、分析或解释代码，没有实际修改时，可以不创建编号。
- 不得为过去的任务自动补编号。

---

## 3. DEVELOPMENT_LOG.md
路径：

```
docs/DEVELOPMENT_LOG.md
```

用途：

记录每次实际代码修改和开发任务。

每次完成代码修改后，原则上都需要更新。

每条记录应包含：

```
开发任务编号
日期
任务状态
任务目标
实际完成内容
主要修改文件或模块
遇到的问题
解决方式
执行的测试
测试结果
未完成事项
风险或注意事项
Git Commit ID
```

Commit ID规则：

- 尚未提交时填写“未提交”。
- 已经提交时填写真实 Commit ID。
- 不得提前编造 Commit ID。
- 后续完成提交后，可以补充真实 Commit ID。

---

## 4. CHANGELOG.md
路径：

```
CHANGELOG.md
```

用途：

记录正式版本变化，不记录所有日常代码修改。

仅在以下情况更新：

- 用户明确要求发布新版本。
- 用户明确确认一个版本节点。
- 用户明确要求整理版本更新内容。

记录内容可包括：

```
版本号
日期
新增
修改
修复
数据库变化
相关 Commit
相关 Git Tag
```

限制：

- 不要自动提高版本号。
- 不要自行决定发布版本。
- 不要自动创建 Git Tag。
- 不得虚构版本号、发布日期、Commit ID 或 Tag。

---

## 5. DATABASE_CHANGES.md
路径：

```
docs/DATABASE_CHANGES.md
```

仅在发生以下变化时更新：

- 新建或删除数据表。
- 新增、删除或修改字段。
- 修改字段类型。
- 修改默认值或约束。
- 修改索引或外键。
- 修改 RLS。
- 修改数据库函数。
- 创建或修改 Migration。
- 修改 Storage 配置。
- 修改本地数据库结构。
- 修改同步、备份或恢复相关结构。

数据库变更编号格式：

```
DB-YYYYMMDD-序号
```

每条记录应包含：

```
数据库变更编号
日期
修改原因
实际修改内容
涉及的表和字段
Migration 文件路径
对现有数据的影响
风险
回滚方式
测试内容
测试结果
相关 DEV 编号
相关 Commit ID
```

如果本次任务没有数据库变化，不要修改该文件。

---

## 6. DECISIONS.md
路径：

```
docs/DECISIONS.md
```

用途：

记录重要技术或产品决策。

决策编号格式：

```
ADR-001
ADR-002
```

仅在发生重要决策时更新，例如：

- 更换核心技术方案。
- 调整整体代码架构。
- 改变数据访问方式。
- 改变数据库设计原则。
- 改变权限设计。
- 改变存储方案。
- 引入重要核心依赖。
- 放弃已经采用的重要方案。

每条记录应包含：

```
决策编号
日期
状态
背景
最终决定
选择原因
考虑过的其他方案
对当前代码的影响
对后续开发的影响
```

普通样式修改、文案调整和小型 Bug 修复不需要记录。

---

## 7. ROADMAP.md
路径：

```
docs/ROADMAP.md
```

本文件用于后续记录整体规划。

当前阶段规则：

- 保持为空。
- 不读取代码后自动生成规划。
- 不填写功能列表。
- 不标记任何完成状态。
- 不根据现有页面或文件推测功能是否完成。
- 只有用户明确要求整理规划时，才能写入。

---

## 8. 测试记录规则
每次代码修改后，应执行与修改范围相符的测试。

只能记录实际执行过的测试。

每条测试记录必须说明：

```
执行的命令或检查
测试范围
测试结果
失败信息
是否完成修复
仍然存在的风险
```

规则：

- 不得写“测试通过”，除非测试实际执行并通过。
- 无法执行测试时，应写明原因。
- 测试失败时，应保留真实失败结果。
- 不得删除、跳过或修改测试，只为了让结果显示通过。
- 数据库测试结果同时记录到 `DATABASE_CHANGES.md`。
- 普通代码测试结果记录到 `DEVELOPMENT_LOG.md`。
- 未修改代码时，不要求执行完整项目测试。

---

## 9. Git规则
未经用户明确要求，不要执行：

```
git commit
git push
git tag
git reset
git revert
git checkout
git switch
git rebase
git merge
```

可以使用以下只读命令检查状态：

```
git status
git diff
git log
git branch
```

规则：

- Git保存真实代码修改。
- 文档解释每次修改的原因和结果。
- 不得修改或重写已有Git历史。
- 不得自动提交。
- 不得自动上传。
- 不得自动切换分支。
- 不得自动创建版本Tag。

---

## 10. 安全规则
不得在任何记录文件中写入：

- 密码。
- Access Token。
- API Key。
- Service Role Key。
- 数据库密码。
- `.env` 的真实内容。
- 私密连接字符串。
- 用户认证信息。
- 用户私人数据。

可以记录“调整了环境变量配置”，但不得记录具体密钥值。

---

## 11. 每次任务结束时的判断规则
按照以下逻辑判断文档更新：

```
实际修改代码
→ 更新 DEVELOPMENT_LOG.md

用户明确发布版本
→ 更新 CHANGELOG.md

数据库结构或权限变化
→ 更新 DATABASE_CHANGES.md

发生重要技术决策
→ 更新 DECISIONS.md

用户明确要求整理规划
→ 更新 ROADMAP.md
```

只更新实际涉及的文档。

---

## 12. 当前初始化状态
当前阶段：

- 只建立记录规则。
- 所有记录从后续新的代码修改任务开始。
- 不补写历史记录。
- 不生成当前版本号。
- 不生成当前规划。
- 不自动分析 Git 历史并生成文档。
- 不自动填写任何功能完成状态。
